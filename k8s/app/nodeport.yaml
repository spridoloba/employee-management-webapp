apiVersion: v1
kind: Service
metadata:
  name: emapp-service
  namespace: emapp
spec:
  selector:
    app: emapp
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080
  type: NodePort