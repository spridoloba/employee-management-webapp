apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-service
spec:
  selector:
    app: {{ .Release.Name }}
  ports:
    - port: {{ .Values.app.config.appPort }}
      targetPort: {{ .Values.app.config.appPort }}
      nodePort: {{ .Values.app.config.nodePort }}
  type: NodePort